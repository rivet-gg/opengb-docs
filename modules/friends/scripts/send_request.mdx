---
title: "Send Request"
description: "Send a friend request to another user."
sidebarTitle: "Send Request"
---

TODO:

- Schema

| Source                                                                                                      | Name     | Public     |
| ----------------------------------------------------------------------------------------------------------- | -------- | ---------- |
| [Source](https://github.com/rivet-gg/opengb-registry/tree/main/modules/friends/scripts/send_request.ts) | `send_request` | Yes |

<RequestExample>

```bash curl
  curl --request POST \
    --url https://dog-api.kinduff.com/api/facts
```

```typescript TypeScript
let response = await fetch("https://dog-api.kinduff.com/api/facts", {});
```

</RequestExample>

<ResponseExample>

```json Response
{ "status": "success" }
```

</ResponseExample>

## Request

```typescript
export interface Request {
  userToken: string;
  targetUserId: string;
}
```

## Response

```typescript
export interface Response {
  friendRequest: FriendRequest;
}
export interface FriendRequest {
  id: string;
  senderUserId: string;
  targetUserId: string;
  createdAt: string;
  declinedAt: null | string;
  acceptedAt: null | string;
}
```
