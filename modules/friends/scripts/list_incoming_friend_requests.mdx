---
title: "List Incoming Friend Requests"
description: "List all friend requests received by a user."
sidebarTitle: "List Incoming Friend Requests"
---

TODO:

- Schema

| Source                                                                                                      | Name     | Public     |
| ----------------------------------------------------------------------------------------------------------- | -------- | ---------- |
| [Source](https://github.com/rivet-gg/opengb-registry/tree/main/modules/friends/scripts/list_incoming_friend_requests.ts) | `list_incoming_friend_requests` | Yes |

<RequestExample>

```bash curl
  curl --request POST \
    --url https://dog-api.kinduff.com/api/facts
```

```typescript TypeScript
let response = await fetch("https://dog-api.kinduff.com/api/facts", {});
```

</RequestExample>

<ResponseExample>

```json Response
{ "status": "success" }
```

</ResponseExample>

## Request

```typescript
export interface Request {
  userToken: string;
}
```

## Response

```typescript
export interface Response {
  friendRequests: FriendRequest[];
}
export interface FriendRequest {
  id: string;
  senderUserId: string;
  targetUserId: string;
  createdAt: string;
  declinedAt: null | string;
  acceptedAt: null | string;
}
```
