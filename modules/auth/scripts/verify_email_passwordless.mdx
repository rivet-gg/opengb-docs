---
title: "Verify Email Passwordless"
description: "Verify a user's email address with a one-time verification code."
sidebarTitle: "Verify Email Passwordless"
---

TODO:

- Schema

| Source                                                                                                      | Name     | Public     |
| ----------------------------------------------------------------------------------------------------------- | -------- | ---------- |
| [Source](https://github.com/rivet-gg/opengb-registry/tree/main/modules/auth/scripts/verify_email_passwordless.ts) | `verify_email_passwordless` | Yes |

<RequestExample>

```bash curl
  curl --request POST \
    --url https://dog-api.kinduff.com/api/facts
```

```typescript TypeScript
let response = await fetch("https://dog-api.kinduff.com/api/facts", {});
```

</RequestExample>

<ResponseExample>

```json Response
{ "status": "success" }
```

</ResponseExample>

## Request

```typescript
export interface Request {
  verificationId: string;
  code: string;
}
```

## Response

```typescript
export interface Response {
  token: TokenWithSecret;
}
export interface TokenWithSecret {
  token: string;
  id: string;
  type: string;
  meta: any;
  createdAt: string;
  expireAt: null | string;
  revokedAt: null | string;
}
```
