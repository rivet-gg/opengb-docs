---
title: "Quickstart"
---

import InstallPrerequisites from "/snippets/install-prerequisites.mdx";
import Install from "/snippets/install.mdx";

<Steps>
    <Step title="Install prerequisites">
        <InstallPrerequisites />
    </Step>
    <Step title="Install Open Game Backend">
        <Install />
    </Step>
    <Step title="Create project">
        Create a new project by running the following command:

        ```sh
        opengb init
        ```

        <Info>
            This creates a `backend.yaml` config file. See the project configuration [here](/concepts/project-config).
        </Info>
    </Step>
    <Step title="Add modules">
        The default project comes with a set of modules preinstalled. Install the `friends` module by running the following command:

        ```sh
        opengb module add friends
        ```

        This adds the `friends` module to the `backend.yaml` file. You can also install modules manually by adding them to this config.

        <Info>
            See a list of available modules [here](/modules).
        </Info>
    </Step>
    <Step title="Run development server">
        The development server is great for testing your scripts locally. It will hot reload when you make changes to your backend.

        ```sh
        opengb dev
        ```
    </Step>
    <Step title="Generate SDK (optional)">
        Open Game Backend will generate an SDK with type-safe methods to call your scripts.
        
        Replace `<output>` with the path where you want to generate the SDK. Replace `<target>` with the target language.

        ```sh
        opengb sdk generate --output <output> <target>
        ```

        See the [SDK docs](/concepts/sdk) for instructions on how to use the generated SDK.

        <Info>
            Godot, Unity, Unreal, and Rust SDKs coming soon.
        </Info>
    </Step>
    <Step title="Open in Postman/Insomnia/Firecamp/etc (optional)">
        The [OpenAPI](https://www.openapis.org/) specification is generated at `_gen/openapi.json`. You can import this file into your favorite API client to start making requests to your server.
    </Step>
    <Step title="Deploy server (optional)">
        <Info>
            Documentation coming soon.
        </Info>
    </Step>
    <Step title="Next steps">
        <CardGroup>
            <Card title="See available modules ->" href="/modules/overview" />
            <Card title="Learn about registries ->" href="/concepts/registries" />
            <Card title="Learn about the CLI ->" href="/concepts/cli" />
            <Card title="Write your own module ->" href="/build/crash-course" />
        </CardGroup>
    </Step>

</Steps>
